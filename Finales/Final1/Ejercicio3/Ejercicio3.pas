Program Ejercicio3;

(*Escribir las cabeceras de los subprogramas que implementarían las siguientes tareas:
    1. Verificar si un elemento ingresado por teclado existe en una matriz de números naturales.
    2. Retornar las posición en la que se encuentra un elemento ingresado por teclado en una matriz de números
       naturales.
    3. Obtener el valor mínimo en una matriz de números naturales.
*)
const
    max=20;
Type
    TM = array[1..max,1..max] of word;
    TV = array[1..2] of byte;
//1) function Esta(mat:TM;x:word):boolean;
//2) function BuscarPos(Mat:TM;x:word):TV;
//3) function ObtenerMin(mat:TM):word;
begin

  
end.